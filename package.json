{
  "name": "vault-lens",
  "version": "1.0.0",
  "description": "Self-hosted, agent-maintained markdown knowledge base for personal capture, retrieval, surfacing, and decisions.",
  "main": "index.js",
  "scripts": {
    "vault:setup": "bash bin/setup-vault.sh",
    "vault:health": "uv run python tools/vault_health.py",
    "vault:compile": "uv run python tools/vault_compile_cache.py --vault-root .",
    "vault:dream": "uv run python tools/vault_dream.py --vault-root .",
    "vault:search": "uv run python tools/vault_search.py search --vault-root . --query",
    "vault:tasks": "uv run python tools/vault_tasks.py sync-from-vault --vault-root .",
    "vault:heartbeat": "uv run python tools/vault_heartbeat.py --vault-root .",
    "vault:trajectory": "uv run python tools/vault_trajectory.py export --vault-root . --run-id",
    "x:fetch": "uv run python tools/x_content.py",
    "test:vault-infra": "uv run python -m unittest tools/test_vault_infra.py",
    "vault:web": "bun tools/vault_query_server.mjs",
    "rebuild:dashboards": "uv run python tools/rebuild_dashboards.py",
    "enrich:browser:recent": "bun tools/enrich_with_browser.mjs . 120 4 30",
    "telegram:sync": "uv run python tools/telegram_inbox.py sync --vault-root .",
    "telegram:run": "uv run python tools/telegram_inbox.py run --vault-root .",
    "telegram:webhook:test": "uv run python tools/telegram_inbox.py webhook --vault-root . --session-name telegram-webhook-test",
    "cloud:deploy": "bash cloud/deploy_telegram_webhook.sh",
    "cloud:sync-codex-auth": "bash cloud/sync_codex_auth_to_s3.sh",
    "cloud:sync-state": "bash cloud/sync_state_to_s3.sh",
    "test": "bun run test:vault-infra"
  },
  "keywords": [
    "personal-knowledge-base",
    "markdown",
    "obsidian",
    "telegram-bot",
    "aws-lambda",
    "agents",
    "retrieval"
  ],
  "author": "Suraj Ranganath",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/suraj-ranganath/VaultLens.git"
  },
  "bugs": {
    "url": "https://github.com/suraj-ranganath/VaultLens/issues"
  },
  "homepage": "https://github.com/suraj-ranganath/VaultLens#readme",
  "type": "commonjs",
  "dependencies": {
    "@googleworkspace/cli": "^0.22.5"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
